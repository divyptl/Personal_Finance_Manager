<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingTop="8dp"
    android:paddingBottom="4dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Credentials are stored locally on your device."
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/inputApiKey"
        style="@style/InputField"
        android:layout_marginTop="4dp"
        android:hint="@string/label_api_key"
        android:inputType="text"
        android:importantForAutofill="no" />

    <EditText
        android:id="@+id/inputClientCode"
        style="@style/InputField"
        android:layout_marginTop="10dp"
        android:hint="@string/label_client_code"
        android:inputType="text"
        android:importantForAutofill="no" />

    <EditText
        android:id="@+id/inputPin"
        style="@style/InputField"
        android:layout_marginTop="10dp"
        android:hint="@string/label_pin"
        android:inputType="numberPassword"
        android:importantForAutofill="no" />

    <EditText
        android:id="@+id/inputTotpSecret"
        style="@style/InputField"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="8dp"
        android:hint="@string/label_totp_secret"
        android:inputType="textVisiblePassword"
        android:importantForAutofill="no" />
</LinearLayout>
